# Конспект по правам доступа в Linux

## 1. Чтение прав (`ls -l`)

```bash
ls -l

📌 Пример вывода:

-rw-r--r--  1 user user 1234 Mar 17 12:34 settings.conf
drwxr-xr-x  2 user user 4096 Mar 17 12:34 my_folder

🔹 Разбираем:
 • -rw-r--r-- → Права доступа
 • d → Папка (- вместо d – обычный файл)
 • rw- → Владелец (user) → может читать и писать
 • r-- → Группа (group) → только чтение
 • r-- → Остальные (others) → только чтение

⸻

2. Таблица прав (буквы)

Символ Значение
r Чтение (read)
w Запись (write)
x Исполнение (execute)
- Нет прав



⸻

3. Изменение прав (числа)

📌 Кодировка прав (восьмеричная система):
 • 4 – чтение (r)
 • 2 – запись (w)
 • 1 – выполнение (x)

📌 Таблица числовых прав:

Число Права
7 rwx (чтение, запись, выполнение)
6 rw- (чтение, запись)
5 r-x (чтение, выполнение)
4 r– (только чтение)
3 -wx (запись, выполнение)
2 -w- (только запись)
1 –x (только выполнение)
0 — (нет прав)

📌 Как поменять права (числовой метод):

chmod 750 file.txt  # Владелец: rwx, Группа: r-x, Остальные: ---
chmod 644 file.txt  # Владелец: rw-, Группа: r--, Остальные: r--
chmod 777 file.txt  # Все могут всё (опасно)



⸻

4. Изменение прав (буквенный метод)

📌 Формат: chmod [кто]+/-[права] файл
 • u – владелец (user)
 • g – группа (group)
 • o – остальные (others)
 • a – все (all)

📌 Примеры:

chmod u+rwx file.txt  # Дать владельцу все права
chmod g-w file.txt    # Убрать у группы право на запись
chmod o-r file.txt    # Запретить остальным читать файл
chmod a+x script.sh   # Разрешить всем запускать скрипт



⸻

5. Изменение владельца и группы (chown, chgrp)

📌 chown – Изменение владельца файла

chown newuser file.txt  # Сделать владельцем файла пользователя "newuser"
chown newuser:newgroup file.txt  # Изменить владельца и группу

📌 chgrp – Изменение группы

chgrp newgroup file.txt  # Назначить группе "newgroup" владение файлом
chgrp newgroup file.txt  # Назначить группе "newgroup" владение файлом

